<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springcloud.cn.serviceblog.dao.CommentDao">
    <select id="queryCommentByBlog" resultType="com.springcloud.cn.serviceblog.entity.Comment">

      select * from comment
      <where>
          <if test="blogId != null and blogId != ''">
              and blog_id = #{blogId}
          </if>
          <if test="userId != null and userId != ''">
              and user_id = #{userId}
          </if>
      </where>
    </select>

    <insert id="add" parameterType="com.springcloud.cn.serviceblog.entity.Comment" >
        insert into comment(id, user_id, blog_id, user_name, blog_comment, create_time)
        values(#{id}, #{userId}, #{blogId}, #{userName}, #{blogComment}, #{createTime})
    </insert>
</mapper>